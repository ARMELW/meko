# Settings specification

- General Settings:
   - Choose the currency used on the platform (Euro, Dollar, etc.).
   - Select the display language (French, English, etc.).
- Security:
   - Change the password (requires the old password and parental validation via a 6-digit code sent by email).