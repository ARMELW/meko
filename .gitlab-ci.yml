# stages:
#   - build

# variables:
#   DOCKER_TLS_CERTDIR: ""
#   IMAGE_TAG: "1.0"

# # Étape 1 : Build et push de l’image Docker
# build-image:
#   stage: build
#   image: docker:latest
#   tags:
#     - front
#   services:
#     - docker:dind
#   #variables:
#     # REGISTRY_USER, REGISTRY_PASS, REGISTRY_HOST, IMAGE_DOCKER
#   before_script:
#     - mkdir -p /etc/docker/certs.d/registry.meko.ac
#     # Ajoute ton certificat racine (à stocker dans une variable GitLab CI, par exemple CERT_CA)
#     - echo "$CERT_CA" > /etc/docker/certs.d/registry.meko.ac/ca.crt
#     - cat /etc/docker/certs.d/registry.meko.ac/ca.crt
#     - echo "$REGISTRY_PASS" | docker login "$REGISTRY_HOST" -u "$REGISTRY_USER" --password-stdin
#     - echo "tafiditra ato"
#   script:
#     - docker build -t "$IMAGE_DOCKER:$IMAGE_TAG" .
#     - echo "Pushing image to registry..."
#     # - docker push "$IMAGE_DOCKER:$IMAGE_TAG"
#   only:
#     - develop
image: docker:24.0.5

variables:
  DOCKER_HOST: unix:///var/run/docker.sock

build:
  stage: build

  script:
    - docker info
  tags:
    - front
